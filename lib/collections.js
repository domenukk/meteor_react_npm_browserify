//Files in the lib folder run before anything else. :)

Collection = new Mongo.Collection('collection');

/**
 * A list of all avaliable collections as string
 * @type {string[]}
 */
Collections = [
    "collection"
];

// TODO: Schemas etc. here. :)