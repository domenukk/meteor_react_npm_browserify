const {
    Toolbar,
    ToolbarGroup,
    FlatButton,
    FontIcon,
    Avatar
    } = MUI;

Footer = React.createClass({
    render() {
        return (
            <p>Fill in footer</p>
        )
    }
});